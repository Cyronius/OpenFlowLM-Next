[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "oflm-test"
version = "0.6.2"
description = "A comprehensive testing framework for OpenFlowLM that validates the functionality of various AI model categories including Language, Embedding, Audio, and Vision models."
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
keywords = ["oflm", "openflowlm", "q4nx", "npu", "model-testing", "vision", "audio", "text"]
classifiers = [
    "Environment :: Console",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Topic :: Software Development :: Build Tools"
]
dependencies = [
    "annotated-types==0.8.0",
    "anyio==4.14.2",
    "certifi==2026.7.22",
    "distro==1.9.0",
    "h11==0.16.0",
    "jiter==0.16.0",
    "openai==3.2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[project.scripts]
oflm-test = "oflm_test:main"

[tool.setuptools]
packages = ["oflm_test"]

[tool.setuptools.package-data]
oflm_test = ["test_files/**/audio/*.mp3", "test_files/**/*.png", "test_files/**/*.jpg", "test_files/**/*.jpeg", "test_files/**/*.mp3", "test_files/*.json"]
